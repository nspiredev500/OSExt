I may want to implement my own libc, as newlib still seems to produce quite big binaries and the
other libcs aren't that easy to port.

Also I can just stub all functions and implement them properly as I implement them in my kernel.




